<%= form_for(@annee) do |f| %>
  <% if @annee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@annee.errors.count, "error") %> prohibited this annee from being saved:</h2>

      <ul>
      <% @annee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :slug %><br />
    <%= f.text_field :slug %>
  </div>
  <div class="field">
    <%= f.label :university_id %><br />
    <%= f.number_field :university_id %>
  </div>
  <div class="field">
    <%= f.label :order %><br />
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
